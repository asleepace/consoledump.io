---
import { cn } from '@/lib/utils'
import { BookOpenText } from 'lucide-react'
interface Props {
  type: string
  className?: string
  size?: number
}

const props = Astro.props
---

<BookOpenText
  size={props.size}
  className={cn('text-neutral-600', props.className)}
/>
