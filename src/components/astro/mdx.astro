---
import CodeBlock from './code-block.astro'
import CodeInline from './code-inline.astro'
import Icon from './icon.astro'
import SiteFooter from './site-footer.astro'

//  NOTE: This file is loaded in `./docs/[slug].astro` and passed
//  as props to `<Content {component} />`. This allows overriding
//  custom elements.

export const components = {
  pre: CodeBlock,
  code: CodeInline,
  SiteFooter: SiteFooter,
  Icon: Icon,
  // You can also override other elements
  // blockquote: CustomBlockquote,
  // img: CustomImage,
}
---
